spring:
  application:
    name: plantcloud-backend
  profiles:
    active: dev

server:
  port: 8080

strategy:
  rollback-enhancement:
    enabled: true
    shadow-recording-enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
